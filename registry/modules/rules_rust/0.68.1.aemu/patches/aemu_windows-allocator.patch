diff -urN a/ffi/rs/allocator_library/allocator_library_windows.rs b/ffi/rs/allocator_library/allocator_library_windows.rs
--- a/ffi/rs/allocator_library/allocator_library_windows.rs	1969-12-31 16:00:00.000000000 -0800
+++ b/ffi/rs/allocator_library/allocator_library_windows.rs	2026-02-27 10:57:19.933637907 -0800
@@ -0,0 +1,60 @@
+#![no_std]
+#![allow(warnings)]
+#![allow(internal_features)]
+#![feature(rustc_attrs)]
+
+unsafe extern "C" {
+    #[rustc_std_internal_symbol]
+    fn __rdl_alloc(size: usize, align: usize) -> *mut u8;
+
+    #[rustc_std_internal_symbol]
+    fn __rdl_dealloc(ptr: *mut u8, size: usize, align: usize);
+
+    #[rustc_std_internal_symbol]
+    fn __rdl_realloc(ptr: *mut u8, old_size: usize, align: usize, new_size: usize) -> *mut u8;
+
+    #[rustc_std_internal_symbol]
+    fn __rdl_alloc_zeroed(size: usize, align: usize) -> *mut u8;
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc12___rust_alloc")]
+fn __rust_alloc(size: usize, align: usize) -> *mut u8 {
+    unsafe {
+        return __rdl_alloc(size, align);
+    }
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc14___rust_dealloc")]
+fn __rust_dealloc(ptr: *mut u8, size: usize, align: usize) {
+    unsafe {
+        return __rdl_dealloc(ptr, size, align);
+    }
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc14___rust_realloc")]
+fn __rust_realloc(ptr: *mut u8, old_size: usize, align: usize, new_size: usize) -> *mut u8 {
+    unsafe {
+        return __rdl_realloc(ptr, old_size, align, new_size);
+    }
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc19___rust_alloc_zeroed")]
+fn __rust_alloc_zeroed(size: usize, align: usize) -> *mut u8 {
+    unsafe {
+        return __rdl_alloc_zeroed(size, align);
+    }
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc35___rust_no_alloc_shim_is_unstable_v2")]
+fn __rust_no_alloc_shim_is_unstable_v2() {}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc26___rust_alloc_error_handler")]
+fn __rust_alloc_error_handler(size: usize, align: usize) {
+    panic!();
+}
+
+#[unsafe(export_name = "_RNvCskdKJRKLKjqM_7___rustc42___rust_alloc_error_handler_should_panic_v2")]
+static mut __rust_alloc_error_handler_should_panic_v2: u8 = 1;
+
+// Legacy names just in case
+#[unsafe(no_mangle)] static mut __rust_alloc_error_handler_should_panic: u8 = 1;
diff -urN a/ffi/rs/allocator_library/BUILD.bazel b/ffi/rs/allocator_library/BUILD.bazel
--- a/ffi/rs/allocator_library/BUILD.bazel	2025-12-09 17:08:33.000000000 -0800
+++ b/ffi/rs/allocator_library/BUILD.bazel	2026-02-27 10:57:19.933832306 -0800
@@ -11,10 +11,7 @@
 )

 srcs = select({
-    # Windows doesn't support weak symbol linkage.
-    # If someone can make this work on Windows, please do!
-    # For now we will silently not supply any symbols, because it would be very messy to conditionally define the default allocator library on toolchains depending on the platform.
-    "@platforms//os:windows": ["empty.rs"],
+    "@platforms//os:windows": ["allocator_library_windows.rs"],
     "//conditions:default": ["allocator_library.rs"],
 })

diff -urN a/ffi/rs/BUILD.bazel b/ffi/rs/BUILD.bazel
--- a/ffi/rs/BUILD.bazel	2025-12-09 17:08:33.000000000 -0800
+++ b/ffi/rs/BUILD.bazel	2026-02-27 10:57:19.934010023 -0800
@@ -18,6 +18,7 @@
 alias(
     name = "default_allocator_libraries",
     actual = select({
+        "@platforms//os:windows": ":allocator_libraries_with_mangling_support",
         "@rules_rust//rust/settings:experimental_use_allocator_libraries_with_mangled_symbols_on": ":allocator_libraries_with_mangling_support",
         "//conditions:default": ":empty_allocator_libraries",
     }),
